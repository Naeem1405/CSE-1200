#include<bits/stdc++.h>
using namespace std;

int getnum(int len){
    return 10 * len;
}
int max(int len){
    
}

int main(){
    int t;
    cin >> t;
    while(t--){
        int len;
        cin >> len;
        int num;
        cin >> num;

    }
}